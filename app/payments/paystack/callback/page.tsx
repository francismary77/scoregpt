import type{Metadata}from"next";import Link from"next/link";import{SiteHeader}from"@/components/site-header";import{SiteFooter}from"@/components/site-footer";import{createCheckoutService}from"@/modules/billing/checkout-runtime";import{whatsappUrl}from"@/config/brand";export const metadata:Metadata={title:"Payment result",robots:{index:false,follow:false}};export default async function Page({searchParams}:{searchParams:Promise<{reference?:string}>}){const{reference}=await searchParams;let success=false,orderNumber="";if(reference)try{const result=await createCheckoutService().verify(reference);success=result.order.status==="paid";orderNumber=result.order.orderNumber}catch{}return <><SiteHeader/><main className="checkout-page"><section className="section"><div className="container"><div className="auth-panel"><span className="eyebrow">Setup payment</span><h1>{success?"Payment confirmed":"Payment could not be confirmed"}</h1><p>{success?`Order ${orderNumber} is confirmed. We are ready to collect your platform setup information.`:"Your order has not been fulfilled. Please retry verification later or contact us with your order reference."}</p>{success?<a className="button" href={whatsappUrl(`Hello FABRO TECH LIMITED, my 9ja Football AI setup payment for order ${orderNumber} is confirmed. I am ready to continue onboarding.`)} target="_blank" rel="noopener noreferrer">Continue setup on WhatsApp</a>:<Link className="button" href="/sales">Return to package details</Link>}</div></div></section></main><SiteFooter/></>}
