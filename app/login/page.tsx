import type{Metadata}from"next";import{AuthShell}from"@/components/auth-shell";import{LoginForm}from"@/components/auth-forms";export const metadata:Metadata={title:"Member Login",description:"Access your football intelligence member dashboard."};export default async function Page({searchParams}:{searchParams:Promise<{next?:string}>}){const{next}=await searchParams;return <AuthShell eyebrow="Member access" title="Welcome back." copy="Continue to your prediction access, saved activity and membership overview."><LoginForm next={next}/></AuthShell>}
