import type{Metadata}from"next";import{notFound}from"next/navigation";import{SiteHeader}from"@/components/site-header";import{SiteFooter}from"@/components/site-footer";import{PlatformCheckout}from"@/components/platform-checkout";import{businessPackages}from"@/config/pricing";import{paymentService}from"@/modules/billing/application";export const metadata:Metadata={title:"Platform Setup Checkout",description:"Review your selected platform package and available payment methods."};export function generateStaticParams(){return businessPackages.map(item=>({packageId:item.id}))}export default async function Page({params}:{params:Promise<{packageId:string}>}){const{packageId}=await params,selected=businessPackages.find(item=>item.id===packageId);if(!selected)notFound();return <><SiteHeader/><main className="checkout-page"><section className="checkout-hero"><div className="container"><span className="eyebrow">Business platform checkout</span><h1>Complete your platform order.</h1><p>Review your edition, choose an available payment method and follow the verification instructions.</p></div></section><section className="section"><div className="container"><PlatformCheckout packageName={selected.name} amount={selected.founderPrice} methods={paymentService.getPaymentMethods()}/></div></section></main><SiteFooter/></>}
