import type{Metadata}from"next";import{AuthShell}from"@/components/auth-shell";import{ResetPasswordForm}from"@/components/auth-forms";export const metadata:Metadata={title:"Choose a New Password"};export default function Page(){return <AuthShell eyebrow="Secure recovery" title="Choose a new password." copy="Use at least eight characters and keep your account password private."><ResetPasswordForm/></AuthShell>}
