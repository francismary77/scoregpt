import type{Metadata}from"next";import{AuthShell}from"@/components/auth-shell";import{ForgotPasswordForm}from"@/components/auth-forms";export const metadata:Metadata={title:"Reset Password"};export default function Page(){return <AuthShell eyebrow="Account recovery" title="Reset your password." copy="Enter your email to preview the future secure recovery experience. No email provider is connected in this phase."><ForgotPasswordForm/></AuthShell>}
