import{brand,whatsappUrl}from"@/config/brand";import{commercialJourney}from"@/config/pricing";export function WhatsAppLink({message=commercialJourney.generalWhatsAppMessage,label="Chat With Us on WhatsApp",className="button"}:{message?:string;label?:string;className?:string}){return <a className={className} href={whatsappUrl(message)} target="_blank" rel="noopener noreferrer" aria-label={`${label} at ${brand.contactPhone}`}><span aria-hidden="true">◉</span> {label}</a>}export function FloatingWhatsApp({message=commercialJourney.generalWhatsAppMessage}:{message?:string}){return <a className="floating-whatsapp" href={whatsappUrl(message)} target="_blank" rel="noopener noreferrer" aria-label={`Chat with ${brand.companyName} on WhatsApp at ${brand.contactPhone}`}><span aria-hidden="true">◉</span><b>WhatsApp Us</b></a>}
