"use client";import{useEffect,useState}from"react";import Link from"next/link";import{useRouter}from"next/navigation";import type{MembershipDisplay,User}from"@/modules/account/domain";import{membershipService,predictionEntitlementService,authService}from"@/modules/account/application";import{entitlementConfig}from"@/config/application";import{ProtectedView}from"./protected-view";import{AllowanceStatus,PremiumBadge,UpgradeCard}from"./membership-ui";import{DisplayState}from"./display-state";
function useMembership(user:User){const[data,setData]=useState<{membership:MembershipDisplay;remaining:number}|null>(null);useEffect(()=>{Promise.all([membershipService.getDisplay(user.id),predictionEntitlementService.decide(user,"registered")]).then(([membership,access])=>setData({membership,remaining:access.remaining??entitlementConfig.freePredictionAllowance}))},[user]);return data}
export function DashboardView(){return <ProtectedView>{user=><Dashboard user={user}/>}</ProtectedView>}function Dashboard({user}:{user:User}){const data=useMembership(user);if(!data)return <DisplayState type="loading" title="Loading membership" copy="Resolving your prediction access…"/>;return <div className="member-content"><div className="member-welcome"><div><span className="eyebrow">Member dashboard</span><h1>Welcome back, {user.displayName}.</h1><p>Your membership, prediction access and useful football shortcuts in one place.</p></div>{data.membership.hasPremiumAccess?<PremiumBadge/>:<span className="member-tier">Free member</span>}</div><div className="dashboard-grid"><article className="membership-card"><small>Membership</small><h2>{data.membership.label}</h2><p>{data.membership.hasPremiumAccess?"Complete demonstration intelligence access.":"Selected member intelligence with a configurable welcome allowance."}</p><span>Status: {data.membership.status}</span></article><article className="access-card"><small>Prediction access</small>{data.membership.hasPremiumAccess?<><h2>Full access</h2><p>Premium demonstration reports are available.</p></>:<AllowanceStatus remaining={data.remaining}/>}</article></div><div className="quick-actions"><Link href="/matches">Today&apos;s predictions <span>→</span></Link><Link href="/matches">Match Centre <span>→</span></Link><Link href="/results">Results <span>→</span></Link><Link href="/pricing">Upgrade to Premium <span>→</span></Link></div><section className="recent-views"><div><span className="eyebrow">Recent activity</span><h2>Viewed intelligence</h2></div><div className="recent-view-row"><span>ARS</span><div><b>Arsenal vs Chelsea</b><small>Demonstration intelligence report</small></div><Link href="/matches/ars-che-demo">View again →</Link></div></section>{!data.membership.hasPremiumAccess&&<UpgradeCard compact/>}</div>}
export function AccountView(){return <ProtectedView>{user=><Account user={user}/>}</ProtectedView>}function Account({user}:{user:User}){const data=useMembership(user),router=useRouter();if(!data)return <DisplayState type="loading" title="Loading account" copy="Preparing your profile…"/>;async function signOut(){await authService.signOut();router.push("/")}return <div className="member-content account-content"><div className="member-welcome"><div><span className="eyebrow">Account</span><h1>Your profile.</h1><p>Review the temporary identity and membership state used in this demonstration.</p></div></div><div className="account-grid"><section><h2>Profile</h2><dl><div><dt>Display name</dt><dd>{user.displayName}</dd></div><div><dt>Email</dt><dd>{user.email}</dd></div><div><dt>Country</dt><dd>Nigeria</dd></div><div><dt>Profile storage</dt><dd>Temporary mock session</dd></div></dl></section><section><h2>Membership</h2><dl><div><dt>Plan</dt><dd>{data.membership.label}</dd></div><div><dt>Status</dt><dd>{data.membership.status}</dd></div><div><dt>Prediction access</dt><dd>{data.membership.hasPremiumAccess?"Full access":`${data.remaining} remaining`}</dd></div></dl>{!data.membership.hasPremiumAccess&&<Link className="button" href="/pricing">Upgrade to Premium</Link>}</section></div><div className="account-actions"><button className="button button-ghost" onClick={signOut}>Sign out</button><small>Password and permanent profile management will be enabled with secure live authentication.</small></div></div>}
export function AdminView(){return <ProtectedView admin>{user=><div className="member-content"><div className="member-welcome"><div><span className="eyebrow">Administration foundation</span><h1>Admin tools.</h1><p>Welcome, {user.displayName}. Role-based access is working in mock mode.</p></div><span className="member-tier">Admin</span></div><DisplayState type="disabled" title="Admin tools coming in a later batch" copy="No user, database, prediction or billing management features are connected yet."/></div>}</ProtectedView>}
