import Link from"next/link";import{brand}from"@/config/brand";import{businessPackages}from"@/config/pricing";import{WhatsAppLink}from"./whatsapp-cta";const ready=["Preferred package","Brand or business name","Desired domain","Logo and brand colours","Intended subscriber pricing","Target launch date"];export function BusinessInquiryForm(){return <div className="inquiry-layout"><section className="inquiry-form"><span className="eyebrow">Before we chat</span><h3>Have these details ready</h3><p>You do not need to submit information on this page. Share only what is needed when you start the WhatsApp conversation.</p><div className="requirements-grid">{ready.map(item=><span key={item}>✓ {item}</span>)}</div></section><aside className="direct-contact"><span className="eyebrow">Ready to discuss your platform?</span><h3>Chat with {brand.companyName}</h3><p>Ask about the right edition, branding, delivery, Managed Platform service or your commercial requirements.</p><WhatsAppLink/><div className="checkout-links">{businessPackages.map(item=><Link key={item.id} href={`/checkout/platform/${item.id}`}>Review payment for {item.name} →</Link>)}</div><small>{brand.contactPhone}</small></aside></div>}
