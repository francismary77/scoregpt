import{formatNaira}from"@/config/pricing";import type{CustomerBillingSnapshot}from"@/modules/billing/customer-billing";import{cancelOwnSubscription,resumeOwnSubscription}from"@/app/account/billing-actions";
const date=(value?:string)=>value?new Date(value).toLocaleDateString("en-NG",{day:"numeric",month:"short",year:"numeric"}):"Not available";
export function CustomerBilling({snapshot}:{snapshot:CustomerBillingSnapshot}){const s=snapshot.subscription;return <section className="customer-billing"><div className="member-welcome"><div><span className="eyebrow">Billing</span><h2>Subscriptions and platform orders</h2><p>Trusted payment and billing status for your account.</p></div></div><article className="billing-card"><h3>Premium Monthly</h3>{s?<><dl><div><dt>Price</dt><dd>₦3,000/month</dd></div><div><dt>Status</dt><dd>{s.cancelAtPeriodEnd?"Non-renewing":s.status}</dd></div><div><dt>Access status</dt><dd>{s.entitlementStatus}</dd></div><div><dt>Current billing period</dt><dd>{date(s.currentPeriodStart)} – {date(s.currentPeriodEnd)}</dd></div><div><dt>Next payment date</dt><dd>{s.cancelAtPeriodEnd?"No further renewal scheduled":date(s.nextBillingAt)}</dd></div><div><dt>Payment provider</dt><dd>Paystack</dd></div></dl>{s.status==="active"&&s.cancelAtPeriodEnd?<form action={resumeOwnSubscription}><button className="button" type="submit">Resume subscription</button><p>Resume the same subscription before cancellation becomes effective. No second subscription is created.</p></form>:s.status==="active"?<details className="cancel-confirm"><summary>Cancel subscription</summary><p>Cancelling stops future renewal. Current paid access continues until the end of this billing period, where applicable.</p><form action={cancelOwnSubscription}><button className="button button-ghost" type="submit">Confirm cancellation at period end</button></form></details>:<p>Subscription management is unavailable for this status. Contact support if you need help.</p>}</>:<><p>No paid consumer subscription is recorded for this account.</p><a className="button" href="/pricing">View Premium</a></>}</article><div className="billing-orders">{snapshot.orders.map(({order,managed})=><article className="billing-card" key={order.id}><h3>{order.packageId==="launch"?"Launch Edition":"Business Edition"}</h3><dl><div><dt>Order reference</dt><dd>{order.orderNumber}</dd></div><div><dt>Setup price</dt><dd>{formatNaira(order.chargedAmountMinor/100)}</dd></div><div><dt>Payment method</dt><dd>{order.paymentMethod==="manual-bank"?"Bank Transfer":"Paystack"}</dd></div><div><dt>Payment status</dt><dd>{order.paymentMethod==="manual-bank"&&order.status!=="paid"?"Awaiting payment confirmation":order.status}</dd></div><div><dt>Payment date</dt><dd>{date(order.paidAt)}</dd></div><div><dt>Onboarding</dt><dd>{order.onboardingStatus}</dd></div><div><dt>Managed Platform</dt><dd>{managed?.status??"Starts after confirmed payment"}</dd></div><div><dt>Included period</dt><dd>{managed?`${date(managed.includedPeriodStart)} – ${date(managed.includedPeriodEnd)}`:"Not started"}</dd></div><div><dt>Monthly fee after inclusion</dt><dd>{formatNaira((order.packageId==="launch"?1800000:2400000)/100)}/month</dd></div><div><dt>Future authorization</dt><dd>{managed?.status==="authorization_required"?"Required":"Not currently required"}</dd></div></dl></article>)}{snapshot.orders.length===0&&<p>No business setup orders are recorded for this account.</p>}</div></section>}
