"use client";import{createContext,useCallback,useContext,useEffect,useState}from"react";import type{AuthState}from"@/modules/account/domain";import{authService}from"@/modules/account/application";import{authChangedEvent}from"@/modules/account/providers";const Context=createContext<{state:AuthState;refresh:()=>Promise<void>}>({state:{status:"checking"},refresh:async()=>{}});export function AuthStateProvider({children}:{children:React.ReactNode}){const[state,setState]=useState<AuthState>({status:"checking"});const refresh=useCallback(async()=>setState(await authService.getAuthState()),[]);useEffect(()=>{queueMicrotask(()=>void refresh());window.addEventListener(authChangedEvent,refresh);return()=>window.removeEventListener(authChangedEvent,refresh)},[refresh]);return <Context.Provider value={{state,refresh}}>{children}</Context.Provider>}export const useAuthState=()=>useContext(Context);
