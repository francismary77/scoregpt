"use client";import{useState}from"react";import Link from"next/link";import{useRouter}from"next/navigation";import{authService}from"@/modules/account/application";import{validateEmail,validatePassword,validateRegistration,type AuthFormErrors}from"@/modules/account/validation";
function Field({label,name,type="text",error,children}:{label:string;name:string;type?:string;error?:string;children?:React.ReactNode}){return <label className="auth-field"><span>{label}</span><input name={name} type={type} aria-invalid={!!error} aria-describedby={error?`${name}-error`:undefined} required/>{error&&<small id={`${name}-error`}>{error}</small>}{children}</label>}
export function RegisterForm(){const[errors,setErrors]=useState<AuthFormErrors>({}),[notice,setNotice]=useState(""),router=useRouter();async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const input={displayName:String(form.get("displayName")??""),email:String(form.get("email")??""),password:String(form.get("password")??""),confirmPassword:String(form.get("confirmPassword")??""),acceptedTerms:form.get("terms")==="on"};const next=validateRegistration(input);setErrors(next);if(Object.keys(next).length)return;await authService.register(input);setNotice("Demo account ready. Opening your dashboard…");router.push("/dashboard")}return <form className="auth-form" onSubmit={submit} noValidate><Field label="Name" name="displayName" error={errors.displayName}/><Field label="Email" name="email" type="email" error={errors.email}/><div className="auth-field-grid"><Field label="Password" name="password" type="password" error={errors.password}/><Field label="Confirm password" name="confirmPassword" type="password" error={errors.confirmPassword}/></div><label className="terms-check"><input type="checkbox" name="terms"/><span>I accept the <Link href="/terms">Terms</Link> and <Link href="/privacy">Privacy Policy</Link>.</span></label>{errors.terms&&<p className="form-error">{errors.terms}</p>}<button className="button auth-submit">Create free account</button>{notice&&<p className="form-notice" role="status">{notice}</p>}<p className="auth-switch">Already have an account? <Link href="/login">Log in</Link></p></form>}
export function LoginForm(){const[error,setError]=useState(""),router=useRouter();async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget),email=String(form.get("email")??""),password=String(form.get("password")??"");const issue=validateEmail(email)??validatePassword(password);if(issue){setError(issue);return}try{await authService.signIn({email,password})}catch{setError("Live credential login is not connected. Use Demo Access below.")}}async function demo(id:string){setError("");await authService.signInDemo(id);router.push("/dashboard")}return <><form className="auth-form" onSubmit={submit} noValidate><Field label="Email" name="email" type="email"/><Field label="Password" name="password" type="password"/><div className="login-options"><label><input type="checkbox"/> Remember me on this device</label><Link href="/forgot-password">Forgot password?</Link></div>{error&&<p className="form-error" role="alert">{error}</p>}<button className="button auth-submit">Log in</button><p className="auth-switch">New here? <Link href="/register">Create account</Link></p></form><div className="demo-access"><span>Demo access · temporary session only</span><div><button onClick={()=>demo("demo-free")}>Free Member</button><button onClick={()=>demo("demo-premium")}>Premium Member</button><button onClick={()=>demo("demo-admin")}>Admin User</button></div><small>No real credentials or permanent user records are used.</small></div></>}
export function ForgotPasswordForm(){const[message,setMessage]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const email=String(new FormData(event.currentTarget).get("email")??""),error=validateEmail(email);if(error){setMessage(error);return}await authService.requestPasswordReset(email);setMessage("Demo only: no reset email was sent. This form is ready for a future secure auth provider.")}return <form className="auth-form" onSubmit={submit} noValidate><Field label="Account email" name="email" type="email"/><button className="button auth-submit">Request reset</button>{message&&<p className="form-notice" role="status">{message}</p>}<p className="auth-switch"><Link href="/login">← Back to login</Link></p></form>}
