export interface AuthFormErrors{displayName?:string;email?:string;password?:string;confirmPassword?:string;terms?:string}const emailPattern=/^[^\s@]+@[^\s@]+\.[^\s@]+$/;export function validateEmail(email:string){return emailPattern.test(email.trim())?undefined:"Enter a valid email address."}export function validatePassword(password:string){return password.length>=8?undefined:"Use at least 8 characters."}export function validateRegistration(input:{displayName:string;email:string;password:string;confirmPassword:string;acceptedTerms:boolean}):AuthFormErrors{const errors:AuthFormErrors={};if(!input.displayName.trim())errors.displayName="Enter your name.";errors.email=validateEmail(input.email);errors.password=validatePassword(input.password);if(input.password!==input.confirmPassword)errors.confirmPassword="Passwords do not match.";if(!input.acceptedTerms)errors.terms="Accept the Terms and Privacy Policy to continue.";return Object.fromEntries(Object.entries(errors).filter(([,value])=>value))}
