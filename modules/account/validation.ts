export interface AuthFormErrors{displayName?:string;email?:string;password?:string;confirmPassword?:string;terms?:string}const emailPattern=/^[^\s@]+@[^\s@]+\.[^\s@]+$/,maximumPasswordBytes=72;export function passwordUtf8ByteLength(password:string){return new TextEncoder().encode(password).byteLength}export function validateEmail(email:string){return emailPattern.test(email.trim())?undefined:"Enter a valid email address."}export function validatePassword(password:string){if(password.length<8)return"Use at least 8 characters.";return passwordUtf8ByteLength(password)<=maximumPasswordBytes?undefined:"Use a password no longer than 72 UTF-8 bytes."}export function validateRegistration(input:{displayName:string;email:string;password:string;confirmPassword:string;acceptedTerms:boolean}):AuthFormErrors{const errors:AuthFormErrors={};if(!input.displayName.trim())errors.displayName="Enter your name.";errors.email=validateEmail(input.email);errors.password=validatePassword(input.password);if(input.password!==input.confirmPassword)errors.confirmPassword="Passwords do not match.";if(!input.acceptedTerms)errors.terms="Accept the Terms and Privacy Policy to continue.";return Object.fromEntries(Object.entries(errors).filter(([,value])=>value))}
