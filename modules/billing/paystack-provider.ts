if(typeof window!=="undefined")throw new Error("Paystack credentials are restricted to the server runtime.");
import type{ConsumerSubscriptionVerification,InitializePaymentInput,ProviderPaymentResult}from"./domain";import type{ConsumerSubscriptionProvider,PaymentGatewayProvider}from"./providers";
const SAFE_BASE="https://api.paystack.co";type PaystackEnvelope={status?:unknown;message?:unknown;data?:unknown};
export function assertPaystackTestNetworkSafety(env:NodeJS.ProcessEnv){if(env.PAYMENTS_ENABLED!=="true")throw new Error("payments_disabled");if(env.PAYMENT_PROVIDER!=="paystack")throw new Error("wrong_payment_provider");if(env.PAYSTACK_ENVIRONMENT!=="test")throw new Error("paystack_test_environment_required");const secret=env.PAYSTACK_SECRET_KEY;if(!secret||!/^sk_test_[A-Za-z0-9_-]{8,}$/.test(secret))throw new Error("valid_paystack_test_key_required");if(secret.startsWith(["sk","live",""].join("_")))throw new Error("live_paystack_key_forbidden");if((env.PAYSTACK_API_BASE_URL??SAFE_BASE)!==SAFE_BASE)throw new Error("invalid_paystack_base_url");return{secret,baseUrl:SAFE_BASE}}
const envelope=(value:unknown):PaystackEnvelope=>{if(!value||typeof value!=="object"||Array.isArray(value))throw new Error("malformed_provider_response");return value as PaystackEnvelope};const object=(value:unknown):Record<string,unknown>=>{if(!value||typeof value!=="object"||Array.isArray(value))throw new Error("malformed_provider_response");return value as Record<string,unknown>},optionalObject=(value:unknown):Record<string,unknown>=>value&&typeof value==="object"&&!Array.isArray(value)?value as Record<string,unknown>:{},string=(value:unknown)=>typeof value==="string"&&value?value:null,integer=(value:unknown)=>typeof value==="number"&&Number.isSafeInteger(value)&&value>0?value:null;
export class PaystackTestProvider implements PaymentGatewayProvider,ConsumerSubscriptionProvider{readonly id="paystack";readonly environment="test"as const;private env:NodeJS.ProcessEnv;private fetcher:typeof fetch;private callbackUrl?:string;constructor(env:NodeJS.ProcessEnv,fetcher:typeof fetch=fetch,callbackUrl?:string){this.env=env;this.fetcher=fetcher;this.callbackUrl=callbackUrl}
 private async request(path:string,init?:RequestInit){const{secret,baseUrl}=assertPaystackTestNetworkSafety(this.env);let response:Response;try{response=await this.fetcher(`${baseUrl}${path}`,{...init,headers:{Authorization:`Bearer ${secret}`,"Content-Type":"application/json",...(init?.headers??{})},signal:AbortSignal.timeout(15000)})}catch{throw new Error("paystack_network_error")};let body:unknown;try{body=await response.json()}catch{throw new Error("malformed_provider_response")};const parsed=envelope(body);if(!response.ok||parsed.status!==true)throw new Error(response.status>=500?"paystack_server_error":"paystack_request_rejected");return parsed.data??{}}
 private async call(path:string,init?:RequestInit){return object(await this.request(path,init))}
 async initialize(request:InitializePaymentInput):Promise<ProviderPaymentResult>{if(request.environment!=="test"||request.providerId!=="paystack")throw new Error("provider_environment_mismatch");const data=await this.call("/transaction/initialize",{method:"POST",body:JSON.stringify({email:request.customerEmail,amount:request.expectedAmountMinor,currency:request.currency,reference:request.reference,callback_url:this.callbackUrl,metadata:request.metadata})}),authorizationUrl=string(data.authorization_url),reference=string(data.reference);if(!authorizationUrl||reference!==request.reference)throw new Error("invalid_provider_response");return{providerId:"paystack",environment:"test",reference,state:"initialized",amountMinor:request.expectedAmountMinor,currency:request.currency,authorizationUrl}}
 async verify(reference:string):Promise<ProviderPaymentResult>{const data=await this.call(`/transaction/verify/${encodeURIComponent(reference)}`),providerReference=string(data.reference),currency=string(data.currency),customer=optionalObject(data.customer),metadata=optionalObject(data.metadata),customerEmail=string(customer.email),trustedMetadata=Object.fromEntries(["order_id","product_key","price_id"].flatMap(key=>{const value=string(metadata[key]);return value?[[key,value]]:[]}));if(!providerReference||typeof data.amount!=="number"||!currency||typeof data.status!=="string")throw new Error("invalid_provider_response");return{providerId:"paystack",environment:"test",reference:providerReference,state:data.status==="success"?"succeeded":"failed",amountMinor:data.amount,currency,...(customerEmail?{customerEmail}:{}),...(Object.keys(trustedMetadata).length?{metadata:trustedMetadata}:{})}}
 async ensureCustomer(userId:string,email:string){if(!userId||!/^\S+@\S+\.\S+$/.test(email))throw new Error("invalid_customer_request");const data=await this.call("/customer",{method:"POST",body:JSON.stringify({email,metadata:{application_user_id:userId}})}),providerCustomerReference=string(data.customer_code);if(!providerCustomerReference)throw new Error("invalid_provider_customer");return{providerCustomerReference}}
 async initializeSubscription(input:Parameters<ConsumerSubscriptionProvider["initializeSubscription"]>[0]){if(input.amountMinor!==300000||input.currency!=="NGN"||input.billingInterval!=="monthly"||input.productKey!=="consumer-premium-monthly")throw new Error("invalid_subscription_catalogue");const plan=this.env.PAYSTACK_CONSUMER_MONTHLY_PLAN_CODE;if(!plan||plan!==input.providerPlanReference||!/^PLN_[A-Za-z0-9]+$/.test(plan))throw new Error("trusted_test_plan_required");const data=await this.call("/transaction/initialize",{method:"POST",body:JSON.stringify({email:input.email,amount:input.amountMinor,currency:input.currency,reference:input.reference,plan,callback_url:this.callbackUrl,metadata:{application_user_id:input.userId,product_key:input.productKey,provider_customer_reference:input.providerCustomerReference,provider_plan_reference:plan}})}),authorizationUrl=string(data.authorization_url),reference=string(data.reference);if(!authorizationUrl||reference!==input.reference)throw new Error("invalid_provider_response");return{reference,authorizationUrl}}
 async verifySubscription(reference:string):Promise<ConsumerSubscriptionVerification>{const data=await this.call(`/transaction/verify/${encodeURIComponent(reference)}`),metadata=(data.metadata&&typeof data.metadata==="object"?data.metadata:{})as Record<string,unknown>,customer=(data.customer&&typeof data.customer==="object"?data.customer:{})as Record<string,unknown>,plan=(data.plan&&typeof data.plan==="object"?data.plan:{})as Record<string,unknown>,providerReference=string(data.reference),providerCustomerReference=string(customer.customer_code),providerPlanReference=string(plan.plan_code)??string(metadata.provider_plan_reference),userId=string(metadata.application_user_id),productKey=string(metadata.product_key),providerSubscriptionReference=string(data.subscription_code);if(!providerReference||typeof data.amount!=="number"||!string(data.currency)||!providerCustomerReference||!providerPlanReference||!userId||!productKey)throw new Error("invalid_provider_response");return{reference:providerReference,providerId:"paystack",environment:"test",status:data.status==="success"?"success":"failed",amountMinor:data.amount,currency:String(data.currency),providerCustomerReference,providerPlanReference,...(providerSubscriptionReference?{providerSubscriptionReference}:{}),userId,productKey}}
 async getSubscription(providerSubscriptionReference:string){if(!/^SUB_[A-Za-z0-9]+$/.test(providerSubscriptionReference))throw new Error("invalid_provider_subscription_identity");const data=await this.call(`/subscription/${encodeURIComponent(providerSubscriptionReference)}`),customer=object(data.customer),plan=object(data.plan),actualReference=string(data.subscription_code),providerCustomerReference=string(customer.customer_code),providerPlanReference=string(plan.plan_code),amount=integer(data.amount??plan.amount),currency=string(data.currency??plan.currency);if(actualReference!==providerSubscriptionReference||!providerCustomerReference||!providerPlanReference||!amount||!currency)throw new Error("invalid_provider_response");return{reference:actualReference,providerId:"paystack",environment:"test",status:data.status==="active"?"success":"failed",amountMinor:amount,currency,providerCustomerReference,providerPlanReference,providerSubscriptionReference:actualReference,userId:"",productKey:"",periodStart:string(data.createdAt??data.created_at)??undefined,periodEnd:string(data.next_payment_date)??undefined,nextBillingAt:string(data.next_payment_date)??undefined}as ConsumerSubscriptionVerification}
 async findSubscriptions(input:{providerCustomerReference:string;providerPlanReference:string}){if(!/^CUS_[A-Za-z0-9]+$/.test(input.providerCustomerReference)||!/^PLN_[A-Za-z0-9]+$/.test(input.providerPlanReference))throw new Error("invalid_subscription_lookup_identity");const customer=await this.call(`/customer/${encodeURIComponent(input.providerCustomerReference)}`),plan=await this.call(`/plan/${encodeURIComponent(input.providerPlanReference)}`),customerId=integer(customer.id),planId=integer(plan.id);if(!customerId||!planId||string(customer.customer_code)!==input.providerCustomerReference||string(plan.plan_code)!==input.providerPlanReference)throw new Error("invalid_provider_response");const listed=await this.request(`/subscription?customer=${customerId}&plan=${planId}&perPage=100`);if(!Array.isArray(listed))throw new Error("malformed_provider_response");return listed.map(value=>{const row=object(value),rowCustomer=object(row.customer),rowPlan=object(row.plan),reference=string(row.subscription_code),customerReference=string(rowCustomer.customer_code),planReference=string(rowPlan.plan_code),amount=integer(row.amount??rowPlan.amount),currency=string(row.currency??rowPlan.currency);if(!reference||!/^SUB_[A-Za-z0-9]+$/.test(reference)||customerReference!==input.providerCustomerReference||planReference!==input.providerPlanReference||!amount||!currency)throw new Error("invalid_provider_response");return{reference,providerId:"paystack",environment:"test",status:row.status==="active"?"success":"failed",amountMinor:amount,currency,providerCustomerReference:customerReference,providerPlanReference:planReference,providerSubscriptionReference:reference,userId:"",productKey:"",periodStart:string(row.createdAt??row.created_at)??undefined,periodEnd:string(row.next_payment_date)??undefined,nextBillingAt:string(row.next_payment_date)??undefined}as ConsumerSubscriptionVerification})}
 async cancelSubscription(providerSubscriptionReference:string){const token=this.env.PAYSTACK_SUBSCRIPTION_EMAIL_TOKEN;if(!providerSubscriptionReference||!token)throw new Error("subscription_cancellation_token_required");await this.call("/subscription/disable",{method:"POST",body:JSON.stringify({code:providerSubscriptionReference,token})});return{status:"cancelled"as const,providerSubscriptionReference}}
 async enableSubscription(providerSubscriptionReference:string){const token=this.env.PAYSTACK_SUBSCRIPTION_EMAIL_TOKEN;if(!/^SUB_[A-Za-z0-9]+$/.test(providerSubscriptionReference)||!token)throw new Error("subscription_enable_token_required");await this.call("/subscription/enable",{method:"POST",body:JSON.stringify({code:providerSubscriptionReference,token})});return{status:"active"as const,providerSubscriptionReference}}
}
export function createConfiguredPaystackProvider(env:NodeJS.ProcessEnv=process.env,fetcher:typeof fetch=fetch){try{assertPaystackTestNetworkSafety(env)}catch{throw new Error("payments_not_safely_enabled")}const site=(env.NEXT_PUBLIC_SITE_URL??"http://localhost:3000").replace(/\/$/,"");let host:string;try{host=new URL(site).hostname}catch{throw new Error("invalid_payment_callback_url")}if(host==="9jafootballai.com.ng"||host==="www.9jafootballai.com.ng")throw new Error("production_site_forbidden_in_test");return new PaystackTestProvider(env,fetcher,`${site}/payments/paystack/callback`)}
