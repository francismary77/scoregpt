if(typeof window!=="undefined")throw new Error("Payment runtime is restricted to the server.");import { BusinessSetupCheckoutService } from "./b2b-checkout";import { SupabaseCheckoutRepository } from "./b2b-checkout-repository";import { createConfiguredPaystackProvider } from "./paystack-provider";import{requirePaymentsEnabled}from"./runtime-config";export function createCheckoutService(){requirePaymentsEnabled();return new BusinessSetupCheckoutService(new SupabaseCheckoutRepository(),createConfiguredPaystackProvider(),"test")}
